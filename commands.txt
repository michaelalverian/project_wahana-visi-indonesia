# For new project
docker-compose build
docker-compose run --rm app django-admin startproject wvi .
docker-compose up

(after 'docker-compose up', open new terminal and run commend below)
docker exec -it wvi_container /bin/bash
    (root) python manage.py migrate


# If project exist
docker-compose up

(after 'docker-compose up', open new terminal and run commend below)
docker exec -it wvi_container /bin/bash
    (root) python manage.py migrate